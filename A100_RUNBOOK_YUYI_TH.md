# คู่มือการรัน A100 — ClonalFrameML บนยูนิต v4c

ฉบับแปลภาษาไทยของ `A100_RUNBOOK_YUYI.md`
**คำสั่งทั้งหมดคงไว้เป็นภาษาอังกฤษตามเดิม — ให้คัดลอกไปใช้ได้ทันที**

> ## สถานะ 21 สิงหาคม 2569 — งานที่ 1 ควรทำหากมีเวลา งานที่ 2 เสร็จแล้ว
>
> **งานที่ 1 (ClonalFrameML) กำลังรันบนเวิร์กสเตชันอยู่ด้วย** แต่ช้า คือ 22 คอร์
> ทำได้ราว 16 replicon-unit ต่อชั่วโมง รวมราว 10 ชั่วโมงสำหรับทั้ง 176 ยูนิต
> เครื่อง A100 มีคอร์มากกว่าราว 3 เท่า น่าจะเสร็จใน 3–4 ชั่วโมง
> **หากมีเวลา รบกวนช่วยรันให้หน่อย เราจะใช้ผลจากเครื่องที่เสร็จก่อน**
>
> **ไม่มีความเสี่ยงที่ผลจะชนกัน** ผลลัพธ์ของคุณอยู่บนเครื่อง A100 ส่วนผลจาก
> เวิร์กสเตชันอยู่ที่นี่ ส่วนที่ทับซ้อนกันจะกลายเป็นการตรวจสอบความทำซ้ำได้
> ข้ามฮาร์ดแวร์ ซึ่งมีค่าเป็นผลลัพธ์ในตัวเอง คือ ข้อมูลนำเข้าเดียวกัน คนละเครื่อง
> ค่า R/θ, δ และ ν ตรงกันหรือไม่
>
> **รบกวนแจ้ง Peera เมื่อเริ่มรัน** เพื่อจะได้หยุดการรันบนเวิร์กสเตชันและ
> คืนคอร์ให้งานอื่น
>
> **งานที่ 2 (สองยูนิตที่ขาดหาย) — เสร็จแล้ว ข้ามได้เลย** `strain_1_L1_36`
> และ `strain_1_L1_37` ถูกดึงกลับมาจาก Drive เมื่อ 21 สิงหาคม 2569 เรียบร้อยแล้ว
>
> ### ถ้าดาวน์โหลดอะไรไปแล้ว กรุณาดึงสองไฟล์นี้ใหม่
>
> ทั้งสองไฟล์แก้ไขเมื่อ 21 สิงหาคม 2569 **หลังจาก**ที่ส่งฉบับแรกไปแล้ว
> ส่วนไฟล์ข้อมูล (alignment, Clusters, Summaries) ไม่มีการเปลี่ยนแปลง
> ไม่ต้องดาวน์โหลดใหม่
>
> ```bash
> cd ~/v4c_cfml
> rclone copy gdrive_ph:wfsnps-v4c-results/clonalframe_nu_bp.py . -P
> rclone copy gdrive_ph:wfsnps-v4c-results/A100_RUNBOOK_YUYI.md . -P
> ```
>
> ตรวจว่าได้สคริปต์ฉบับใหม่จริง คำสั่งนี้ต้องได้ค่ามากกว่า 0
> (ฉบับเก่าจะได้ 0):
>
> ```bash
> grep -c CFML_CONDA_SH clonalframe_nu_bp.py     # ต้องได้ 1 ขึ้นไป ไม่ใช่ 0
> ```
>
> ถ้าได้ `0` แปลว่า rclone ยังเก็บไฟล์เก่าไว้ ให้ `rm clonalframe_nu_bp.py`
> แล้วรันคำสั่ง copy ใหม่
>
> ### แก้อะไรบ้าง และทำไมฉบับเก่าถึงรันไม่ได้
>
> 1. **พาธ conda และชื่อ env ถูกฝังตายไว้เป็นของเครื่องต้นทาง**
>    (`/home/phemarajata/miniforge3`, env `cfml` / `bp-gubbins` /
>    `snp-phylogeny`) ซึ่งไม่มีอยู่บนเครื่อง A100 ทุกยูนิตจะพังที่
>    `conda activate` ไม่ว่าจะติดตั้งอะไรไว้ก็ตาม ตอนนี้ตั้งค่าทับได้แล้ว ดู §1.2
> 2. **สคริปต์เรียก `iqtree2` โดยเฉพาะ** แต่ env iqtree บนเครื่อง A100 มีแต่
>    `iqtree` ทำให้พัง*หลังจาก* snp-sites รันไปแล้ว ตอนนี้ตรวจหาชื่อที่มีอยู่จริง
> 3. **บั๊ก layout v4c** — สคริปต์แอบไปอ้างอิงพาธของพาร์ทิชันชุดเก่า
>    คือรันข้อมูลผิดชุดโดยไม่ขึ้น error
> 4. **ไฟล์ alignment ไม่ได้อยู่บน Drive** ตอนที่ส่งเอกสารฉบับแรก ขั้นตอน §2.1
>    จึงรันไม่จบ ตอนนี้อัปโหลดครบ 172 ไฟล์และตรวจสอบแล้ว
>    **แต่ให้ทำ §2.1 ขั้นที่ 0 ก่อนอยู่ดี** — เครื่อง A100 อาจมีไฟล์อยู่แล้ว
>    ซึ่งจะประหยัดการดาวน์โหลด 17 GB ไปทั้งหมด

**สำหรับ:** Yuyi **เขียนเมื่อ:** 20 สิงหาคม 2569
**ติดต่อ:** Peera (ไม่อยู่จนถึงราว 27 สิงหาคม 2569)

เอกสารนี้อ่านจบในตัว ไม่จำเป็นต้องรู้ความเป็นมาของโครงการ **มีงานเดียว** คือ §2
กรุณาอ่าน §0 และตารางกับดักใน §4 ก่อนเริ่ม เพราะรูปแบบความล้มเหลวหลายอย่าง
ในที่นี้ดูเหมือนว่าสำเร็จ หรือดูเหมือนเป็นปัญหาคนละเรื่อง

---

## 0. กำลังทำอะไร และทำไมจึงสำคัญ

เราแบ่งจีโนม *Burkholderia pseudomallei* ราว 2,900 ตัวอย่างออกเป็น "ยูนิต"
86 กลุ่ม แล้วรัน Gubbins กับแต่ละกลุ่มเพื่อตัดผลของการแลกเปลี่ยนสารพันธุกรรม
ออกก่อนสร้างต้นไม้วิวัฒนาการ Gubbins รายงานค่าเดียวต่อยูนิตคือ **r/m**
และค่านั้นคือเกณฑ์ *เพียงอย่างเดียว* ที่ตัดสินว่ายูนิตใดใช้งานได้

บนพาร์ทิชัน**ชุดเก่า** เราเคยรันเครื่องมืออีกตัวคือ ClonalFrameML กับ 46 ยูนิต
แล้วเปรียบเทียบกัน ปรากฏว่าเครื่องมือทั้งสอง **ให้คำตอบไม่ตรงกันว่ายูนิตใดดี**
ค่าสหสัมพันธ์อันดับอยู่ที่ +0.30 และ **8 จาก 46 ยูนิตเปลี่ยนผลรับ/ไม่รับ**
ขึ้นอยู่กับว่าถามเครื่องมือตัวไหน ถ้าผลนี้ยังคงอยู่ แปลว่าเกณฑ์คัดกรองขึ้นอยู่กับ
เครื่องมือ ซึ่งเป็นข้อค้นพบเกี่ยวกับ*ระเบียบวิธี* ไม่ใช่เกี่ยวกับยูนิตเหล่านั้น

**งานที่ 1 คือหาคำตอบว่าผลนี้ยังคงอยู่บนพาร์ทิชันปัจจุบัน (v4c) หรือไม่**
ยูนิตชุดเก่ามีชื่อและสมาชิกต่างออกไป ผลเดิมจึงไม่สามารถโอนมาใช้ได้

**งานที่ 1 เป็นงานเดียว** (งานที่ 2 ใน §3 เสร็จแล้วเมื่อ 21 สิงหาคม 2569
คงส่วนนั้นไว้เป็นบันทึกเท่านั้น)

> **งานที่สามที่เคยวางแผนไว้ถูกยกเลิกแล้ว** เดิมคือการรันยูนิตมิสซิสซิปปีใหม่
> เพื่อตัดตัวอย่างที่ปนเปื้อน (`SRR30648681`) ออก แต่ตรวจสอบเมื่อ 20 สิงหาคม 2569
> แล้วพบว่าตัวอย่างนั้นไม่อยู่ในชุด v4c ตั้งแต่แรก **ไม่ต้องรันใหม่**
> หากมีใครขอให้ทำ กรุณาชี้มาที่ย่อหน้านี้

---

## 1. สิ่งที่ต้องมีก่อน

เครื่องคือ DGX Station A100: 64 คอร์จริง (128 คอร์เชิงตรรกะ) แรมราว 480 GB
**GPU ไม่ได้ช่วยเร่งอะไรเลยในงานนี้** ทุกขั้นตอนใช้ CPU ล้วน ไม่ต้องปรับแต่งเพื่อ GPU

ต้องมี rclone ที่ตั้งค่า `gdrive_ph:` ไว้แล้ว และ **พื้นที่ว่างราว 200 GB**
(`df -h .`) ส่วนที่เหลือติดตั้งตามขั้นตอนด้านล่าง

### 1.1 สร้าง conda environment

ตรวจเครื่อง A100 เมื่อ 21 สิงหาคม 2569 พบว่ามี `iqtree` และ `snp-sites`
อยู่คนละ env กัน แต่ **ไม่มี ClonalFrameML เลย** แทนที่จะเชื่อมสาม env เข้าด้วยกัน
ให้ติดตั้งทั้งสามตัวไว้ใน env เดียว:

```bash
mamba create -y -n cfml-v4c -c bioconda -c conda-forge clonalframeml snp-sites iqtree
```

ตรวจสอบ — ต้องเจอ `ClonalFrameML`, `snp-sites` และ **อย่างน้อยหนึ่งใน**
`iqtree`/`iqtree2`:

```bash
conda run -n cfml-v4c bash -c 'for t in ClonalFrameML snp-sites iqtree iqtree2; do printf "%-14s " $t; command -v $t || echo MISSING; done'
```

เจอชื่อ iqtree แค่ชื่อเดียวก็ไม่เป็นไร สคริปต์จะตรวจหาเองตอนรัน
แต่**ต้องเป็น IQ-TREE เวอร์ชัน 2** เพราะเวอร์ชัน 1.x ไม่รองรับ flag `-fconst`
ที่ไปป์ไลน์นี้ต้องใช้ ตรวจด้วย
`conda run -n cfml-v4c iqtree --version | head -1`

### 1.2 ตั้งค่าให้สคริปต์ชี้มาที่ conda ของเครื่องคุณ ไม่ใช่ของเครื่องต้นทาง

ค่าเริ่มต้นในสคริปต์เป็นของเครื่องที่เขียนมันขึ้นมา
(`/home/phemarajata/miniforge3`, env `cfml` / `bp-gubbins` / `snp-phylogeny`)
ซึ่งไม่มีอยู่บนเครื่องนี้ **ถ้าไม่ตั้งค่าสี่บรรทัดนี้ ทุกยูนิตจะพังที่
`conda activate`**:

```bash
export CFML_CONDA_SH=$HOME/miniforge3/etc/profile.d/conda.sh
export CFML_ENV_CFML=cfml-v4c
export CFML_ENV_TREE=cfml-v4c
export CFML_ENV_SNP=cfml-v4c
```

**ต้องตั้งค่าใน shell เดียวกับที่จะสั่งรัน** ถ้าเปิด terminal ใหม่ต้องตั้งใหม่
หรือใส่ไว้ใน `~/.bashrc` เพื่อไม่ต้องคอยจำ

---

## 2. งานที่ 1 — ClonalFrameML กับ 86 ยูนิต v4c

### 2.1 เตรียมข้อมูลนำเข้า

ClonalFrameML ต้องใช้ไฟล์ alignment เต็มของแต่ละยูนิต (`*.core.full.aln`
รวมราว 17 GB) ส่วนไฟล์อื่นมีขนาดเล็ก

**ขั้นที่ 0 — ตรวจหาบนเครื่อง A100 ก่อน** เครื่องนี้เคยรันไปป์ไลน์นี้มาก่อน
จึงอาจยังมีไฟล์อยู่ ซึ่งจะประหยัดการดาวน์โหลด 17 GB ไปได้ทั้งหมด:

```bash
find ~ -name "*.core.full.aln" -path "*Clusters*" 2>/dev/null | head
```

ถ้ามีไฟล์ขึ้นมา ให้จดโฟลเดอร์ที่บรรจุโฟลเดอร์ `cluster_*` ไว้ แล้วใช้เป็น
`L1v4c_out/Clusters` ในขั้นถัดไป (จะทำ symlink ก็ได้) **แล้วข้ามการดาวน์โหลด
alignment ไปเลย**

**ขั้นที่ 1 — ไฟล์ขนาดเล็ก จำเป็นเสมอ:**

```bash
mkdir -p ~/v4c_cfml && cd ~/v4c_cfml
rclone copy gdrive_ph:wfsnps-v4c-results/snp/Clusters ./L1v4c_out/Clusters -P \
  --exclude "*.core.full.aln"
rclone copy gdrive_ph:wfsnps-v4c-results/snp/Summaries ./L1v4c_out/Summaries -P
rclone copy gdrive_ph:wfsnps-v4c-results/clonalframe_nu_bp.py . -P
```

**ขั้นที่ 2 — ไฟล์ alignment เฉพาะกรณีที่ขั้นที่ 0 ไม่พบอะไร:**

```bash
rclone copy gdrive_ph:wfsnps-v4c-results/snp/Clusters ./L1v4c_out/Clusters -P \
  --include "*.core.full.aln"
```

อัปโหลดครบทั้ง 172 ไฟล์และตรวจสอบแล้วเมื่อ 21 สิงหาคม 2569 ขนาดราว 17 GB
ใช้เวลาพอสมควร หากถูกขัดจังหวะให้รันซ้ำได้ rclone จะทำต่อจากเดิม

**จุดตรวจ — อย่าไปต่อจนกว่าทั้งสามคำสั่งจะให้ผลตามนี้:**

```bash
ls L1v4c_out/Clusters | wc -l                      # ควรได้ 176
wc -l < L1v4c_out/Summaries/recombination_rm.tsv   # ควรได้ 89  (88 ยูนิต + หัวตาราง)
ls L1v4c_out/Clusters/*/*.core.full.aln | wc -l    # ควรได้ 172
```

**ตัวเลขที่ไม่ตรงกันนี้ถูกต้องทั้งหมด ไม่มีอันไหนคือดาวน์โหลดไม่ครบ**
*(แก้เมื่อ 21 สิงหาคม 2569 หลังคุณยุ้ยพบว่าไฟล์สรุปมี 88 ยูนิต)*

ไปป์ไลน์นี้รัน **สองครั้ง** คือรันจริงบน A100 ได้ **88 ยูนิต** และรันควบคุมบน
เวิร์กสเตชันได้ **86 ยูนิต** (ขาด `strain_1_L1_36` กับ `strain_1_L1_37`)
ไฟล์บน Drive จึงปนกัน — **ไฟล์สรุปเป็นฉบับ A100 (88 ยูนิต)** แต่
**ไฟล์ alignment เป็นชุดเวิร์กสเตชัน (86 ยูนิต x 2 replicon = 172 ไฟล์)**

สคริปต์จึงอ่านได้ 88 ยูนิต หา alignment เจอ 86 ยูนิต แล้วขึ้นข้อความ:

```
MISSING alignment: strain_1_L1_36 chr1
MISSING alignment: strain_1_L1_37 chr1
```

**สองบรรทัดนี้เป็นเรื่องปกติ ข้ามได้เลย** ไม่ใช่ error และไม่มีอะไรเสียหาย

ถ้าจำนวน alignment **ต่ำกว่า 172** แสดงว่า rclone ยังไม่เสร็จจริง ให้รันซ้ำ
ถ้าได้ **176** แสดงว่าเจอไฟล์ของ A100 เองในขั้นที่ 0 ซึ่งดียิ่งกว่า
จะได้ 88 งานแทนที่จะเป็น 86

### 2.2 ทดลองรันแบบไม่เขียนไฟล์ก่อน

```bash
python3 clonalframe_nu_bp.py --layout v4c --plan --replicon chr1
```

**บรรทัดสุดท้ายควรขึ้นว่า `86 job(s); 4 concurrent x 4 threads`**
(หรือ `88 job(s)` ถ้ามี alignment ของ A100 เองครบ)

ถ้าขึ้น `6 job(s)` แสดงว่าลืมใส่ `--layout v4c` และกำลังจะรันพาร์ทิชันชุดเก่า
ถ้าขึ้น `0 job(s)` แสดงว่าหาไฟล์ alignment ไม่เจอ ให้ตรวจว่าอยู่ใน `~/v4c_cfml`
และ `L1v4c_out/Clusters` อยู่ใต้โฟลเดอร์นั้นโดยตรง

### 2.3 รัน chr1

เริ่มจาก chr1 ก่อน เป็นครึ่งหนึ่งของงานทั้งหมด และจะบอกเราได้ว่าใช้เวลาเท่าไร
ก่อนจะลงมือส่วนที่เหลือ

```bash
cd ~/v4c_cfml
nohup python3 clonalframe_nu_bp.py --layout v4c --run --replicon chr1 \
      --jobs 12 --threads 8 > cfml_v4c_chr1.log 2>&1 &
```

`--jobs 12 --threads 8` คือ 96 จาก 128 คอร์เชิงตรรกะ เหลือพื้นที่ไว้บ้าง
**อย่าตั้ง `--jobs` เกิน 16** ดูกับดัก T2

**เวลาที่ใช้คาดเดายาก** เพราะ ClonalFrameML ใช้เวลาตามทั้งจำนวนตัวอย่างและ
ความยาว alignment และยูนิตเหล่านี้มีตั้งแต่ 7 ถึง 159 จีโนม ตรวจดูหลังผ่านไป
หนึ่งชั่วโมง:

```bash
ls -d cfml/*__chr1 2>/dev/null | wc -l                 # โฟลเดอร์ที่สร้างแล้ว
ls cfml/*__chr1/cfml.em.txt 2>/dev/null | wc -l        # ยูนิตที่เสร็จแล้ว
```

แล้วประมาณเวลารวมจากตรงนั้น **รบกวนแจ้ง Peera ว่าเสร็จไปกี่ยูนิตหลังหนึ่งชั่วโมง**
นั่นเป็นข้อมูลที่มีประโยชน์ที่สุดที่ส่งกลับมาได้ ถ้าผ่านไปสองชั่วโมงแล้วเสร็จ
ไม่ถึง 3 ยูนิต ให้หยุดแล้วแจ้ง ดีกว่าปล่อยให้รันไปหลายวัน

การรันนี้ **ทำต่อจากเดิมได้** ยูนิตที่มีไฟล์ `cfml.em.txt` แล้วจะถูกข้าม
การหยุดแล้วเริ่มใหม่จะเสียเฉพาะยูนิตที่กำลังรันค้างอยู่

### 2.4 แล้วจึงรัน chr2

ทำหลังจาก chr1 เสร็จแล้วเท่านั้น:

```bash
nohup python3 clonalframe_nu_bp.py --layout v4c --run --replicon chr2 \
      --jobs 12 --threads 8 > cfml_v4c_chr2.log 2>&1 &
```

### 2.5 สรุปผลและส่งกลับ

```bash
python3 clonalframe_nu_bp.py --layout v4c --report --replicon chr1 > TIER1_3_clonalframe_v4c.txt
tar -czf cfml_v4c_results.tar.gz cfml/ TIER1_3_clonalframe_v4c.txt cfml_v4c_chr*.log
rclone copy cfml_v4c_results.tar.gz gdrive_ph:wfsnps-v4c-results/ -P
```

ส่วนที่ต้องดูในรายงานคือหัวข้อ **CONCORDANCE** ตัวเลขที่สำคัญคือค่า Spearman rho
และจำนวนยูนิตที่เปลี่ยนคำตัดสิน

---

## 3. งานที่ 2 — เสร็จแล้วเมื่อ 21 สิงหาคม 2569 ไม่ต้องทำอะไร

*คงไว้เป็นบันทึกเท่านั้น* `strain_1_L1_36` และ `strain_1_L1_37` เคยขาดหายจาก
ตารางระยะห่างบนเวิร์กสเตชัน แต่ดึงกลับมาจาก Drive ได้โดยตรง เพราะตารางตัวแปร
`.core.tab` และไฟล์ GFF ของ Gubbins อยู่ที่นั่นมาตลอด ขาดเพียงไฟล์ alignment
ขนาด 90 MB ซึ่งการวิเคราะห์ส่วนนั้นไม่ได้ใช้ ตอนนี้ตารางระยะห่างครอบคลุมครบ
ทั้ง 88 ยูนิต / 176 replicon-unit แล้ว

---

## 4. กับดัก — อ่านก่อนจะเริ่มไล่หาสาเหตุใด ๆ

ทุกข้อในตารางนี้เคยทำให้โครงการเสียเวลาจริง และส่วนใหญ่ไม่ได้แสดงอาการ
ตรงกับสาเหตุที่แท้จริง

| กับดัก | อาการที่จะเห็น | วิธีแก้ |
|---|---|---|
| **ไฟล์นำเข้าเป็น CRLF** | exit code 141 และ log ขนาด **0 ไบต์** ดูเหมือนรันแล้วพังทันที แต่ไม่ใช่ — เป็นเพราะ pipe ตาย | `file <input>` ถ้าขึ้น CRLF ให้รัน `sed -i 's/\r$//' <file>` |
| **Gubbins/CFML ชนกันเมื่อรันพร้อมกัน** | ได้ตัวเลขผิด **โดยไม่มี error ใด ๆ** เพราะไฟล์ชั่วคราวถูกเขียนลงโฟลเดอร์ปัจจุบัน ไม่ใช่ตาม prefix จึงทับกันเอง | สคริปต์แยกโฟลเดอร์ให้แต่ละยูนิตอยู่แล้ว **แต่อย่ารันสคริปต์สองชุดจากโฟลเดอร์เดียวกัน** |
| **ชื่อรันยาวเกินไป** | RAxML segfault แล้วตัวครอบรายงานว่า *"Unable to fit model to data"* ซึ่งฟังดูเหมือนปัญหาข้อมูล | เป็นข้อจำกัด **128 ตัวอักษร** ของชื่อรัน ให้พาธสั้นเข้าไว้ อยู่ใน `~/v4c_cfml` อย่าซ้อนลึกกว่านี้ |
| **ค่า seed เป็นศูนย์** | ยูนิตหนึ่งพังทั้งที่ยูนิตข้างเคียงสำเร็จ และพังซ้ำเดิมเมื่อรันใหม่ | `randint(0,10000)` ที่ไม่ได้ตั้ง seed จะได้ 0 ราว 1 ใน 10,001 ครั้ง แค่รันยูนิตนั้นใหม่ ไม่ใช่ความล้มเหลวจริง |
| **วนลูป OOM** | ขั้นตอนหนึ่งหน่วยความจำไม่พอ ลองใหม่ แล้วพัง**เหมือนเดิมทุกครั้ง** | เพดานหน่วยความจำบางตัวตั้งตายตัว ไม่ได้เพิ่มตามรอบ การลองใหม่จะไม่มีวันสำเร็จ ให้แจ้งกลับแทน |
| **Nextflow resume** | เกี่ยวข้องเฉพาะกรณีต้องรันไปป์ไลน์ใหม่เท่านั้น | **ห้ามเปลี่ยน cache mode กลางคัน** ถ้าจำเป็นต้อง resume ให้ resume ด้วยคำสั่งเดิมทุกประการ |

### ถ้ามียูนิตล้มเหลว

การที่บางยูนิตล้มเหลวถือเป็นเรื่องปกติ ไม่ใช่วิกฤต ให้ดู log ของยูนิตนั้น:

```bash
tail -30 cfml/<unit>__chr1/run.log
```

แล้วรันเฉพาะยูนิตนั้นใหม่ โดยลบโฟลเดอร์ของมันทิ้งแล้วเรียกสคริปต์อีกครั้ง
(ยูนิตที่เสร็จแล้วจะถูกข้าม จึงทำเฉพาะตัวที่ขาด):

```bash
rm -rf cfml/<unit>__chr1
python3 clonalframe_nu_bp.py --layout v4c --run --replicon chr1 --jobs 1 --threads 16
```

**กรุณาจดว่ายูนิตใดล้มเหลวและเพราะอะไร** ยูนิตที่ประมาณค่าไม่ได้ก็ถือเป็นผลลัพธ์
อย่างหนึ่ง อย่าตัดทิ้งเงียบ ๆ

---

## 5. สิ่งที่ต้องส่งกลับ

1. `cfml_v4c_results.tar.gz` (§2.5)
2. บันทึกสั้น ๆ ระบุ **จำนวนยูนิตที่เสร็จ** **จำนวนและชื่อยูนิตที่ล้มเหลว**
   **เวลารวมที่ใช้** และกับดักใน §4 ข้อใดที่เจอบ้าง

บันทึกนั้นมีประโยชน์กว่าไฟล์ tar กรุณาส่งมาด้วยแม้ทุกอย่างจะราบรื่นดี
