# Handoff — 2026-08-23

Read this first. Working directory `/home/phemarajata/Downloads/snp-mod-local-working`.
Supersedes `HANDOFF_2026-08-21_NIGHT.md`. **Nothing is running.**
Branch `feat/core-shrinkage-and-itol`, working tree clean.

**Pushed through `92c94cf`.** Session commits: `b4fadfa · 12ee79a · 0d65d31 ·
f6d82e9 · 64a8e33 · 04bdc27 · 92c94cf`, plus the commit that corrected this line.
**Run `git status -sb` rather than trusting this sentence** — a push state written
into a document is stale the moment anyone commits, which is the same failure
mode §5 is about.

**Every number in this document was recomputed on 2026-08-23, not copied.** Where
a figure was found stale it is named as stale rather than silently replaced —
§1's "never quote" list and §5 exist for that.

---

## 0. The three frozen foundations — do not drift from these

1. **The analysis basis: `FINAL_BASIS_2026-08-22/`, 85 units, 2,340 genomes**
   (corrected workstation partition). **Run `python3 freeze_basis_bp.py` before
   quoting any number** — **15 checks**, non-zero exit on drift. Currently PASS.
   Take unit membership only from `FINAL_PARTITION.tsv`. Join the panel on
   `unit_membership`, never `subcluster`.

2. **The validation set: 48 registered, 46 scorable.** Register inputs
   `EXPOSURE_OVERRIDES.tsv` and `OUTBREAK_GROUPS.tsv` are **frozen inputs like
   the partition** — changing either triggers a deliberate batched refresh
   (register → regenerate attribution → recompute strata → propagate n), never a
   piecemeal edit. `TRACK0_INTEGRATION_2026-08-23.md` is the model.

3. **The exclusion register is versioned, not absolute.** `PANEL_EXCLUSIONS.tsv`
   rows carry `status`; **`status=retired` is a rescinded decision, kept for the
   record and NOT an active exclusion.** Every reader must filter it — five do.
   Retire via `retire_exclusions_bp.py`, never a hand edit: `*.tsv` is gitignored,
   so a manual change is invisible to git and unreproducible. 42 active, 4
   retired.

**Run `python3 generate_numbers.py`; quote `NUMBERS.tsv` (65 figures). Documents
cite it, they do not restate values.**

## 1. The headline numbers, current

| quantity | value | `NUMBERS.tsv` key |
|---|---|---|
| panel, corrected | **2,959** | `panel.corrected_v4d` |
| panel, region-labelled | **2,946** | `panel.region_labelled` |
| analysed units / genomes | **85 / 2,340** | `units.analysed`, `genomes.analysed` |
| ENA coverage | **41.1%** | `panel.coverage_of_ena` |
| r/m (in-window, Gate 1) | **7.70** (n=47 of 85) | `rm.median_gate1` |
| validation set, **scorable** | **46** (48 registered) | `validation.scorable` |
| country attribution, NN | **10/46 (22%)**, baseline 26% | `attribution.country.nearest_neighbour` |
| country, close relative exists | **2/14** | `…nearest_neighbour.d_lt_0.05` |
| region, modal k=20 | **41/46 (89%)**, κ 0.832 | `attribution.region.modal_k20` + `ladder.region_7way.kappa` |
| region, close relative exists | **14/14** | `attribution.region.modal_k20.d_lt_0.05` |
| ladder: Asia/non-Asia κ | **1.000** | `ladder.asia_vs_not.kappa` |
| abstention: region @ d ≤ 0.462 | **94.3%** on 76.1% coverage (LOO) | `abstention.region.loo_selective_accuracy` |
| cgMLST↔SNP concordance | **+0.861** (Lichtenegger, 85 units, 66 at r≥0.7) | `CGMLST_CONCORDANCE_FROZEN.tsv` |

**Country does not exceed chance.** 22% against a 26% majority baseline, κ 0.193.
Write it that way, not as "reaches 22%".

**The estimator is part of every attribution number.** Country's best estimator is
nearest neighbour; region's is modal k=20. Region under NN is **37/46 (80%)** — a
*different estimator*, not a correction to 89%. `CGMLST_LICHT_ATTRIBUTION.tsv`
stores **only NN**; modal and κ come from `GROUPING_LADDER.tsv`. The estimator is
now in every `NUMBERS.tsv` key so the two cannot be mixed by accident.

**Never quote:** r/m 7.38/7.44/7.26 (other partitions/proxies) · region 93% (n=43)
· country 9/43 · concordance 0.846 (PubMLST/88-hybrid) or 0.864 (both schemes
pooled) · panel 2,976 **or 2,955** · ENA coverage 44% or 41.4% · "7 of 15" source
countries (now **7 of 16**) · downsampling κ **0.89→0.81** (pre-Track-0; now
**0.83→0.77**) · 48 as an attribution denominator.

## 2. What this session did

Everything below was verified by re-running, not asserted. Six commits.

- **The kappa ladder had no file behind it** (`b4fadfa`). The region headline
  — modal k=20, 41/46, κ 0.832 — was cited to `CGMLST_LICHT_ATTRIBUTION.tsv`,
  which is **NN-only and says 37/46 (80%)**. `grouping_test_bp.py` printed the
  ladder and persisted nothing, so every κ sat outside `generate_numbers.py` and
  outside `freeze_basis_bp.py`. Anyone obeying "quote `NUMBERS.tsv`" would have
  published 80%. Now writes `GROUPING_LADDER.tsv` + `GROUPING_PREDICTIONS.tsv`;
  the estimator is in every key; `freeze_basis` cross-checks the two independent
  scorers (12 → 14 checks).
  **Consequence:** the estimator-matched region strata are **14/14 · 8/10 ·
  19/22**, not the NN 11/14 · 6/10 · 20/22 that was shown beside the 89%. Region
  is **perfect where a close relative exists**, against country's 2/14 on those
  same 14 genomes — the depth thesis was being understated.
- **Manuscript outline reconciled onto one cgMLST headline** (`12ee79a`).
  [cg-Licht/46] is now Table 4; [SNP/24] demoted to the core-genome cross-check;
  cg-Pub/29 out of the body. **§6.5's instruction to remove the Lichtenegger
  citation was inverted** — the headline scheme *is* Lichtenegger, verified
  against a fetched PubMed record (PMID 33980649, *J Clin Microbiol*
  2021;59(8):e0009321, 4,221 core targets, matches ours). Also corrected: panel,
  ENA coverage, source countries 7 of 15 → **7 of 16**, W3 marked resolved.
- **The abstention rule built and scored** (`0d65d31`) — D3, finally measured.
  Region: decline calls with no relative closer than **d = 0.462** → coverage
  78.3%, selective accuracy 94.4%, **out-of-sample 94.3%** by leave-one-out
  threshold selection. Declines **both** Sub-Saharan African attractor errors
  (ranks 9 and 10 of 46) and cannot decline the two Mississippi errors (ranks 26,
  27) — **two distinct failure modes, and the rule addresses one.**
  **It fails for country, and that is the more useful result:** the apparent
  +15.8pp lift is *exactly* cancelled by the retained-subset majority baseline
  (37.5% vs 37.5%). Country is not rescued by abstaining.
- **Four exclusions were unevidenced and are retired** (`f6d82e9`, `64a8e33`).
  W8 assumed four genomes were wrongly *present* in the cgMLST pool; measurement
  showed they were wrongly *excluded*. All four pass every gate on the **SPAdes**
  assemblies in use; the decisions were taken on superseded **SKESA** assemblies,
  and `core=na%` was a **transcription error** (the value is in
  `core_cov_unfiltered_pct`; the register read the always-empty
  `core_cov_filtered_pct`). **The "`SRR2896271` fails the species gate" claim is
  refuted** — 0.0135 was SKESA, the assembly in use is 0.0087, inside the
  operative ≤0.012 gate (0.008 is prose, enforced nowhere).
  **No headline moved** — the pool already held all four. Panel 2,955 → **2,959**.
- **Two structural fixes came out of that** and matter more than the decision:
  `freeze_basis_bp.py` now checks the **cgMLST reference pool** (15 checks) — its
  absence is why this passed 14/14 for weeks, because *a genome excluded from the
  analysis can still decide a call as a reference*. And `generate_numbers.py` now
  takes units/genomes from **`FINAL_PARTITION.tsv`**, not the stale cluster file
  minus the registers, which reached 2,340 by coincidence and would have silently
  reported **2,341** after the retirement.
- **Table 3 recomputed; the assembler effect quantified** (`04bdc27`). Burden
  table denominator is **2,946 region-labelled**, not the 2,959 panel (the two
  were briefly, confusingly equal). EAP 2,717 → 2,705, North America 43 → 42;
  **both headline ratios survive exactly** (41× vs South Asia, 33× vs SSA).
  New **W8b**: from four same-isolate pairs, SKESA → SPAdes moves core **+10.8pp**,
  mash **−27%**, length **+1.07 Mb** — larger than the gap between the advisory
  0.008 and enforced 0.012 mash gates, and the panel mixes provenance. Disclosure,
  not correction (Gate 1 already left the Mash proxy; cgMLST never used mash).
  Explicitly **not** "SPAdes is better" — the control has `SRR30648682` passing
  under SKESA and failing under SPAdes at 11.88 Mb.
- **Checked so it need not be re-litigated:** the validation set is **6× enriched**
  for the `new200` batch (16/46 vs 5.9% of panel), which has the worst call-rate
  tail (p05 87.1% vs 95.8%). **It does not bias the distance strata** — Spearman
  −0.247 (n=46, ns), median loci compared flat at 4,042 / 4,040 / 4,024. So the
  d ≥ 0.30 stratum and the abstention threshold on it are **not** call-rate
  artifacts. One named exception: `SRR33748081`, fewest loci (2,520) *and* largest
  distance (0.79048), drives the weak correlation alone.

**Recurring lesson, restated because it paid off again in every commit above:**
the value came from *verifying* — re-running, reading the actual artifact,
checking the assembly the pool actually points at — not from trusting a plausible
number. Three of this session's findings **inverted** the premise they started
from: the ladder file that did not exist, the citation instruction that was
backwards, and the four genomes that were wrongly excluded rather than wrongly
present. Compute every number at the moment you write it.

## 3. Open items, ranked

> **Drafting readiness, assessed 2026-08-23 (`DRAFT_READINESS_2026-08-23.md`):
> draft the Results now; do NOT draft the Methods yet.** The outline is mature
> and every figure is tied to a regenerable key. `METHODS_DRAFT_2026-08-19.md` is
> not: §2.12's body still describes the **88-unit A100 partition** as the analysis
> (8 subsections, ~30% of the draft) though its own preamble correctly says the
> reported basis is 85/2,340; and **§2.12.11a describes a superseded attribution
> analysis** — n=26, SNP-unit modal, "19 of 19 against 58%". The draft contains
> **zero** mentions of Lichtenegger, modal k=20, kappa, leave-outbreak-out or
> abstention. Two live self-contradictions remain (§2.6.3 union coverage;
> §2.12.13 still calls the A100 run "production"). §2.1–2.11 are in good shape
> and r/m is already correct at 7.70.


1. **The reproducibility test** — the standing task
   ([[reproducibility-test-after-methods-final]]): once Methods are frozen,
   re-run the collection end-to-end from primary data and diff every headline.
   Many intermediates were edited in place on 08-21/22/23 and the collection has
   not been re-derived since. Real compute (Gubbins hours; `--shm-size=2g` and
   the zero-seed trap both apply).
2. **The abstention rule needs its threshold re-derived if the validation set
   grows.** Leave-one-out removed *threshold*-selection circularity; it did
   **not** remove **signal**-selection circularity — three signals were compared
   on the same 46 genomes and `nn_distance` was chosen partly because it won.
   d = 0.462 is calibrated-on-46 and is a Lichtenegger-scheme, this-panel number,
   not a species constant. (`ABSTENTION_RESULT_2026-08-23.md` §7)
3. **Exact n=46 accessory control sub-numbers** — re-run `accessory_control_bp.py`.
   The headline is already corrected (accessory country 14/46 = 30% vs core
   10/46 = 22%) and the "fails its controls" conclusion is unchanged; only the
   contig strata, permutation null and the 0/13 close-relative stratum are n=43.
4. **Tech debt: a shared leave-group-out/outbreak helper.** **Four** scorers each
   build their own pool and all four apply leave-outbreak-out
   (`score_cgmlst_lichtenegger`, `score_accessory`, `grouping_test`,
   `downsample_control`); `resolution_curve` needs neither. The next new scorer
   will silently leak without it. **`abstention_rule_bp.py` is the precedent for
   not adding a fifth** — it builds no pool and consumes
   `GROUPING_PREDICTIONS.tsv` instead.
5. ~~**The Vietnam/Georgia lineage question**~~ — **RESOLVED 2026-08-23, and it
   is a result.** `VIETNAM_GEORGIA_RESULT_2026-08-23.md`. It is **five** Georgia
   cases (1983, 1989, 2024 × 3), not two, and they are **autochthonous**: CDC/state
   epidemiology found no recent international travel (Brennan 2025, PMID
   **40835221**, verified against PubMed — the outline's author initial was also
   wrong). The lineage genuinely spans Vietnam and the southeastern USA, confirmed
   by independent in-country Vietnamese isolates. **The Georgia cluster's internal
   max is 8.67 × 10⁻³ and its nearest non-Georgia neighbour is a Vietnam-exposure
   case at 8.91 — a margin of 1.0 locus in 4,221.** Both countries well
   represented, published epidemiology on both sides, and attribution still fails.
   **This also externally validates the leave-outbreak-out design**: an automatic
   rule would have held these out as "same source" and faked a Viet Nam answer of
   2/2. Remaining: reconcile 4 published cases vs 5 deposited Georgia genomes.
6. **Disclose the assembler effect in Methods** (W8b) and fix the transcription
   bug at source: any register row sourced from `NEW200_QC_2026-08-17.tsv` must
   read `core_cov_unfiltered_pct`. Documented in `PANEL_EXCLUSIONS_README.md`;
   **not yet enforced in code.**
7. **IRB approval number** into the Methods (epi team has it — text only).
8. **Deposit the new assemblies** for the data-availability statement.
9. **Method questions for Paper 2**: `+ASC` vs `-fconst` on one unit; Gate 1
   diversity from alignment distances not the Mash proxy — partly done, the
   window is now alignment-derived, but `gate1_from_alignment_bp.py` still
   defaults `--mash` to `trackA_diversity_86units.tsv` (note **86**, an older
   partition).
10. **Minor:** `SRR28096031`'s register reason string is imprecise — it cites a
    core gate whose value the row does not carry, though the exclusion is
    genuinely supported by `ratio = 1.35`. Not in the cgMLST pool; cosmetic.

**Decisions on record (do not relitigate):** don't redo the project
(`REDO_DECISION`); GAMBIT stays on DB 2.2.0, kmerfinder off, no upstream issue
filed; Phase 1 expansion declined; workstation partition is the reported one,
A100 is the reproducibility control; the four retired exclusions stay retired,
`ERR9980356` kept knowingly despite ranking 172/172 on both QC axes.

## 4. Key documents

| file | what |
|---|---|
| **`NUMBERS.tsv`** | every quotable figure, regenerated. Start here |
| `GROUPING_LADDER.tsv` | the kappa ladder, per grouping × estimator |
| `GROUPING_PREDICTIONS.tsv` | per-genome calls + confidence, so strata match their headline's estimator |
| `ABSTENTION_RESULT_2026-08-23.md` | the abstention rule: works for region, fails for country |
| **`DRAFT_READINESS_2026-08-23.md`** | can we draft? Results yes, Methods no — and why |
| `VIETNAM_GEORGIA_RESULT_2026-08-23.md` | the trans-Pacific lineage; the one-locus boundary |
| `EXCLUSION_RECHECK_2026-08-23.md` | the four exclusions re-measured; the assembler effect |
| `FINAL_BASIS_2026-08-22/README.md` | the frozen basis, why, and the rules |
| `PANEL_EXCLUSIONS_README.md` | the register spec: `status`, the cross-check, the `core=na%` trap |
| `TRACK0_INTEGRATION_2026-08-23.md` | validation set 43→46, all deltas |
| `GATE1_ALIGNMENT_RESULT_2026-08-21.md` | r/m 7.70, the window in alignment units |
| `LEAVE_OUTBREAK_OUT_2026-08-23.md` | the leak control + the Vietnam/Georgia find |
| `MANUSCRIPT_OUTLINE_2026-08-21.md` | the paper; read the Figure Provenance Key first |
| `DOWNSAMPLING_CONTROL_RESULT_2026-08-22.md` | region survives 90× rebalancing, κ 0.83→0.77 |
| `RETIRED_2026-08-22/README.md` | what was retired and how to recover it |
| `collaborator/README.md` | Yuyi/A100 materials, live recall condition |

## 5. Traps

**Carried forward, all re-verified today.** Run `generate_numbers.py` before
quoting. Deduplicate on BioSample, never accession. Never compare an NN
attribution number to a modal one. Never quote an accuracy without its baseline
and denominator. Every ENA census unions `read_run` with `result=assembly`.
Verify every PMID against a fetched record. On GAMBIT 3.x gate species on mash to
K96243. Don't record a count until the run producing it has stopped.

**Added this session, each from a defect that actually occurred:**

- **The estimator is in the key.** `CGMLST_LICHT_ATTRIBUTION.tsv` holds **one**
  estimator — whichever `--estimator` was passed — and the frozen copy is NN.
  Modal and κ live in `GROUPING_LADDER.tsv`.
- **Honour `status=retired`** when reading `PANEL_EXCLUSIONS.tsv`. Four rows are
  retired; a reader that ignores `status` re-applies rescinded exclusions.
- **`units.analysed` / `genomes.analysed` come from `FINAL_PARTITION.tsv`**, never
  from `curated_L1v4c_clusters.tsv` minus the registers. That file is
  *pre-correction* (2,352 rows) and still lists `SRR2896257` under
  `strain_1_L1_26`.
- **`CGMLST_CONCORDANCE_FROZEN.tsv` holds BOTH schemes** — 170 rows = 85 units ×
  2. Restrict to one scheme before taking a median: Lichtenegger is **0.861**,
  pooling both gives 0.864.
- **`L1v4c_out/Clusters` is hybrid: 176 dirs = 88 units × 2 replicons**, against a
  frozen basis of **85**. Never glob it for membership.
- **A selective accuracy needs TWO baselines** — random-abstention (= the
  answer-everything accuracy) *and* the majority share of the **retained** subset.
  Abstention changes the class mix, and country's entire apparent gain was that
  change.
- **A QC gate is assembler-dependent.** SKESA → SPAdes moves core +10.8pp and
  mash −27% on the same isolate, and the effect is per-genome — one genome passes
  under SKESA and fails under SPAdes. No global correction factor.
- **`0.008` is not the operative mash gate.** It is prose; code enforces
  **≤ 0.012**.
